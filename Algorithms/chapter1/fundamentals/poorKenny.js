// Kenny tries to stay safe, but somehow everyday something happens. If there is a 10% chance of
// volcano, 15% chance of tsunami, 20% chance of earthquake, 25% chance of blizzard, and 30% chance
// of meteor strike, write function whatHappensToday() to print the outcome. 

function whatHappensToday()
	var chance = Math.random() = 100
	console.log("Today, Kenny will run into...");

	if(chance < 15){
		console.log("tsunami!");
	} else if(chance < 35){
		console.log("A earthquake!");
	} else if(chance < 60){
		console.log("A Blizzard!");
	} else if("")
}